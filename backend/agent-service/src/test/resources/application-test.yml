spring:
  datasource:
    url: jdbc:h2:mem:agent-service;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: true

chanter:
  internal-service-token: test-internal-service-token-for-agent
