spring:
  datasource:
    url: jdbc:h2:mem:media-service;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: true
    locations: classpath:db/migration

chanter:
  jwt:
    secret: chanter-test-jwt-secret-32bytes-min!!
    access-token-ttl: 15m
  internal-service-token: test-internal-service-token-for-media
  media:
    storage-dir: ${java.io.tmpdir}/chanter-media-test
