spring:
  datasource:
    url: jdbc:h2:mem:auth-service;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: true

chanter:
  jwt:
    secret: test-jwt-secret-for-auth-service-smoke-tests-only
    access-token-ttl: 15m
    refresh-token-ttl: 7d
