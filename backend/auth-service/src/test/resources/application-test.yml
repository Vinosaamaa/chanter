spring:
  datasource:
    url: jdbc:h2:mem:auth-service;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: true

chanter:
  events:
    dispatch-enabled: false
  email:
    local-sink: true
    worker-enabled: false
  internal-service-token: test-internal-service-token-for-auth
  jwt:
    secret: test-jwt-secret-for-auth-service-smoke-tests-only
    access-token-ttl: 15m
    refresh-token-ttl: 7d
