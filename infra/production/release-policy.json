{
  "schemaEpoch": 8,
  "reason": "Issue 247 agent V13 replaces BYTEA embeddings with pgvector coordinates, immutable model dimensions and durable generation-fenced rebuild jobs. Older binaries cannot read or safely write this schema, so downgrade to epoch 7 is forbidden. Epoch 7 requires issue 316 native request claims and atomic answer/status outbox delivery; epoch 6 generation-fenced extraction and durable ingestion jobs/cursors; epoch 5 transactional outboxes and projection cursors; epoch 4 private resource lifecycle and terminal deletion markers; epoch 3 versioned password hashes; epoch 2 durable sessions. Restore agent, message and related source/consumer databases, model versions, durable claims/cursors and private objects consistently. Rollback never reverses SQL or changes persistence images."
}
