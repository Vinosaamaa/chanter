{
  "schemaEpoch": 10,
  "reason": "Issue 251 auth V8, community V19, message V12, media V5, agent V14, notification V3 and search V3 add permanent account/server/resource fences, original terminal authority, exact cleanup receipts and nullable erased attribution. Epoch 9 binaries omit these fences or may recreate erased fields, so downgrade is forbidden. Export copies, canonical journal, terminal targets, current/derived scope, deletion jobs, source/recipient cursors, accounting claims and private object disposition must be restored consistently; an older database requires the externally current original journal and source reapply before public writers reopen. Media V6 physical recovery remains a separately verified dependent integration. Issue 249 auth V5-V7 and message V11 add durable operator authority, restrictions, appeals and live media authorization. Older binaries omit current suspension and source checks, so downgrade to epoch 8 is forbidden. Issue 247 agent V13 replaces BYTEA embeddings with pgvector coordinates, immutable model dimensions and durable generation-fenced rebuild jobs. Older binaries cannot read or safely write this schema, so downgrade to epoch 7 is forbidden. Epoch 7 requires issue 316 native request claims and atomic answer/status outbox delivery; epoch 6 generation-fenced extraction and durable ingestion jobs/cursors; epoch 5 transactional outboxes and projection cursors; epoch 4 private resource lifecycle and terminal deletion markers; epoch 3 versioned password hashes; epoch 2 durable sessions. Restore agent, message and related source/consumer databases, model versions, durable claims/cursors and private objects consistently. Rollback never reverses SQL or changes persistence images."
}
