# Copy to an ignored .tfvars file only after creating an Always Free account.
# Values below are placeholders, not deployed infrastructure.
region                 = "REPLACE_WITH_HOME_REGION"
compartment_id         = "REPLACE_WITH_COMPARTMENT_OCID"
availability_domain    = "REPLACE_WITH_AVAILABLE_DOMAIN"
ubuntu_arm64_image_id   = "ocid1.image.REPLACE_WITH_VERIFIED_UBUNTU_24_04_ARM64_IMAGE"
admin_cidr             = "192.0.2.1/32"
ssh_public_key         = "ssh-ed25519 REPLACE_WITH_PUBLIC_KEY"
confirm_always_free_only = false
