# Issue 251 changes

This draft adds private account export snapshots and actual projections for auth, community, message, media, agent, notification and search. Export scope excludes credentials and operator evidence. Retained shared content requires current source authorization. Auth owns recent-login requests, source progress, cancellation, expiry and streamed ZIP integrity. Existing durable delivery carries requests and receipts.

The browser download route consumes a short-lived, one-use cookie bound to the live account/session/job and current refresh cookie. It has a narrow GET-only gateway exception, repeat authorization during streaming, and no URL secret or JavaScript archive buffer. The account-data page provides request, status, cancellation and native browser download controls. Component tests cover uncertain-request retries and late responses after account changes; hosted visual/download-manager proof is pending.

Auth terminal recovery imports the original canonical chain with session/email-token/export closure and participant receipts in one transaction. Restored-session invalidation has a durable idempotent receipt tied to the exact canonical and participant watermark. Identity cleanup remains PENDING. Focused tests cover rollback, replay, incorrect authority, private credentials and strict JSON. External replication is owned by #332.

Coordinated account/Study Server deletion, preservation holds, ownership resolution, the other six recovery handlers, current #249 authority integration, legal/support disclosure work and final hosted cross-service proof remain incomplete. This issue stays open through delivery and operational verification.

The unreleased terminal journal now requires schema 2 and the fixed PRESERVE_MODERATION_RECORDS_V1 disposition in every entry and digest. Original-chain import persists that policy. Shared Java/recovery fixture digest, missing-policy rejection, transactional rollback/replay and private HTTP tests pass. Moderation retention means existing bounded records, not original-file preservation or legal approval.

Hosted Chromium/WebKit exposed differing precedence between anchor and server download filenames. The fixture now uses the same filename and verifies transferred ZIP bytes, with additional screenshots after scrolling to the outcome. The first hosted desktop/mobile screenshots were inspected; final corrected browser proof remains pending. The H2 export fixture now explicitly opts into the accepted vector migration's test database mode; production requirements are unchanged.

A second hosted download check exposed that Chromium/WebKit hand downloads to a network path outside page interception, reaching the visual server's 404 fallback. The visual-only server now serves one exact synthetic archive route and rejects bearer/query credentials; the test verifies completed bytes from the actual browser download manager twice. This remains fixture compatibility proof, separate from the real auth-cookie tests.

Notification now has its owning reapply route, transactional payload/export cleanup, and permanent account/Study Server/resource fences on ordinary upserts. Private HTTP, rollback, replay and delayed event tests exercise actual migrated tables. Existing moderation evidence remains auth-owned. Other participant integrations remain pending.

Agent recovery invalidation now rejects all restored ISSUED/ACCEPTING native requests and erases their evidence atomically with a durable receipt at the exact applied authority. Tests prove rollback, idempotent retry, stale authority rejection, private HTTP validation and inability to claim/settle those requests afterward. Existing UNKNOWN usage and reservations stay unchanged. Agent terminal reapply and the remaining four source handlers are still unfinished.

Community ownership preparation now serializes with actual Study Server creation on a per-account row. Tests pass both commit orders, rollback, unresolved ownership, matching cancellation and permanent closure. The durable coordinator and transfer actions are not yet wired, so this is a boundary checkpoint rather than completed account deletion.

The public draft pages no longer invent support mailboxes or an effective date. Privacy copy separates export expiry from account erasure and leaves operator identity, legal review and retention policy explicitly unconfigured. No real contact or legal approval is claimed. Hosted browser download compatibility now passes on Chromium, Firefox and WebKit at320/390/1280; phone outcome screenshots were inspected, with a further cancellation capture adjusted to show its cleanup text.

Normal notification deletion delivery now uses the same bounded executor and existing durable consumer/outbox. A forced receipt-insert failure rolls payload erasure and the fence back; retry commits one receipt, and duplicate delivery cannot repeat effects. Wrong producer is rejected. Protocol receipt scope binds job, source, target and journal digest; a normal delivery does not advance the recovery prefix. Public auth coordination and the other source participants remain pending.

Search recovery now applies actual resource erasure and persistent indexing fences through both ordinary durable delivery and private journal replay. Account and Study Server results deliberately retain PENDING while canonical ownership reconciliation is unfinished. Fixed the search private-route browser-identity filter gap; private controllers continue to enforce their own internal token. Focused search recovery and existing search smoke tests pass, including a transaction rollback and an overlapping writer. The older pagination fixture now uses nonzero resource UUIDs.

Media deletion now waits for a durable settled-write state. Fresh uploads and actual legacy migration PUTs hold that state open, so an expired worker lease cannot allow deletion to overtake their late bytes. Typed adapter failures distinguish NOT_STARTED, FINISHED and UNKNOWN: pre-dispatch budget rejection and closed local streams remain cleanable; uncertain provider outcomes keep quota and pending cleanup. No elapsed-time or missing-object shortcut declares them settled. Existing uncertain pre-upgrade uploads/migrations are conservatively retained. Object-version/provider-quiescence evidence and final downstream search/moderation completion remain explicit unfinished integration requirements. Focused resource lifecycle, real local storage/worker, migration race and synthetic S3 response tests pass.

Community source recovery now captures immutable terminal-bound Course/channel scope before graph erasure, serves bounded private pages, and refuses stale/missing/uncommitted authority. The actual owning configuration also wires durable preparation/release receipts, permanent account ownership closure and snapshot cancellation. Graph/source retention is still PENDING where outbox/export or authored-record disposition is unfinished. Actual migrated graph rollback/replay, source pagination, private credentials, wrong authority and server-creation fencing tests pass. The public normal deletion coordinator, remaining message/media/agent reapply handlers and accepted moderation union are still outstanding.

Accepted moderation integration now checks permanent canonical ACCOUNT, RESOURCE and STUDY_SERVER authority through the existing live restriction boundary. Temporary expiry/reversal cannot reopen terminal access, and preserved moderation evidence remains intact. The existing user/session locking and refresh rollback behavior are unchanged. Focused auth recovery and moderation regressions pass.

The shared deleted-server scope protocol now validates original journal authority, bounded ordered pages, exact cursors/counts and an incremental SHA-256 digest. Community can stage verified archived scope before replay only in recovery mode; restored older graphs cannot substitute for current authority. Final READY import atomically re-runs pending source reconciliation. Tests prove missing-versus-empty scope, unsigned UUID ordering, changed replay rejection, final-page rollback, and a server absent at the physical recovery point. Historical extra children currently remain retained/PENDING while the separately bound recovery-derived union is being implemented with #332. This does not claim completed source deletion or restore cutover.

Notification now imports the verified deleted-server scope through the shared bounded private route. Final READY import removes legacy Course/channel-only previews, advances the existing source cleanup state atomically, and fences later durable notification delivery without a server ID. Five focused actual-controller/transaction regressions pass, including rollback and repeated import. ACCOUNT receipts now truthfully remain PENDING for authored previews outside the deleted account's own inbox.

Recovery now retains a separately bound historical union derived from the verified restored community database and current archive. Tests cover post-backup server creation, historical child IDs, parent contradictions, unchanged original digest, later-prefix reuse, and transactional graph erasure. Notification recovery imports that union and waits for both kinds before deleting historical previews; rollback and stale recreation tests use actual migrated repositories/private controllers. Shared HTTP access checks classify terminal-account410 as denial rather than outage, and scope reverse lookups have dedicated indexes. Remaining message/media/agent handlers, canonical authored-preview reconciliation and normal public deletion coordination are still in progress.

Rebased onto accepted monitoring #331. The approved AccountDataPage entry has its own 16 KB/6 KB JavaScript and 3 KB/1 KB CSS limits. Existing moderation/browser error allowances and core/vendor/initial-route limits are unchanged. The budget guard rejects duplicate CSS ownership and shared/initial transfer escapes; focused budget tests pass. The hosted signed-in product journey also checks that landing, sign-in and fresh Home never fetch the exact AccountData entry or its CSS. Updated exact production measurements remain a hosted build check.

Search now imports current and recovery-derived deleted-server scope and removes legacy Course/channel-only index rows atomically. Delayed durable writes and legacy replacements cannot recreate verified terminal scope. Actual migrated repository/controller tests cover normal and recovery cleanup, final-page rollback, retries, unrelated rows and stale writes. Community rejects a combined derived scope above 250,000 IDs before hashing or staging either kind; the bounded regression preserves the graph and leaves no derived rows. Four focused search tests and eight community scope tests pass. Remaining source handlers and normal public deletion coordination are still unfinished.

Agent now supplies the real terminal reapply and current/derived scope handlers. Resource cleanup removes chunks, vectors, grants, saved answers/excerpts and matching native evidence while preserving permanent markers. Temporary source exports are invalidated when their content is erased. Ingestion, rebuild, installation, answer, native-request and reservation writes take terminal authority before their row locks. New tests prove rollback, original/current recovery binding, historical legacy cleanup, stale writes and a waiting reservation denied after account deletion while existing UNKNOWN settlement keeps its full reservation. Existing rebuild completion still returns false for deleted work. All 55 focused agent tests pass. Account/server cleanup remains PENDING for retained accounting/attribution and downstream reconciliation; media/message handlers and the normal public coordinator remain in progress. External replica manifest documentation now matches #332 schema 2.

Media now supplies terminal reapply and current/derived scope handlers. Source transactions close access, cancel temporary exports and enqueue at most 256 resources into the existing deletion state machine. Later pages are already unreadable and use ordinary worker claims; unknown PUTs keep their byte reservations. Terminal-first locking prevents late scanner/migration publication. Recovery preserves null namespace authority and rejects a changed known binding. Five new source regressions and 34 existing lifecycle/worker safety checks pass, including a 257-resource page boundary and historical scope rollback. All media cleanup receipts remain PENDING until physical object proof, downstream delivery and retained metadata cleanup finish. Message source handling and normal public coordination remain in progress.

Message terminal cleanup now erases account conversation/relationship rows, authored content and derived FAQ copies in the source transaction, preserves exact ID-only downstream identities, and imports both current and restored server scope kinds. The source head lock precedes social pair and content locks. Late accepted-answer events for terminal authors/channels acknowledge without notification resurrection. All 27 focused message tests pass, including rollback, replay, restored historical scope, concurrent deletion/message insertion, existing block races, questions, FAQs and TA queues. Account/server receipts remain pending for downstream copies and durable payload disposition.

Account deletion now exposes normal authenticated preparation, explicit recent-login confirmation, cancellation and read-only progress. Canonical allocation, auth revocation/export cancellation and six source commands roll back together on a real database insertion failure. Ownership release must arrive before a new job can replace cancellation. The hashed seven-day receipt cookie is exact-job/path, Secure, HttpOnly and SameSite=Strict; it cannot authorize confirmation or ordinary account access. Expired preparation is visible and the existing expiry task releases it through the same outbox. Focused auth/journal/recovery and gateway checks pass, including wrong-job/expiry, passive refresh, replay, rollback, exact route/method and spoofed-header denial. The owning recovery annotation from #332 now omits both lifecycle expiry beans while preserving private handlers. Account content cleanup, owned server/resource coordination, downstream confirmation and hosted end-to-end proof remain unfinished.

Review also identified the remaining accepted-answer/resource race: a queued or already-claimed answer event can update a surviving question after its saved answer is erased. The final union must validate the answer identity/current source authority and prove both delivery cases before readiness.

### Normal source deletion coordination checkpoint

Study Server and resource DELETE routes now return 202 with an opaque pending job. The owning source retains its graph/bytes and records access closure plus one fixed lifecycle outbox request. Auth accepts only the owning producer/kind pair, allocates or reuses the original canonical terminal entry, and commits participant authority, progress and six dispatches atomically. Existing worker tests now supply explicit synthetic canonical delivery before testing physical cleanup, rather than treating the initial public request as erasure authority.

Focused verification passed source preparation rollback and concurrent retry, auth canonical/dispatch rollback and replay, fixed producer/receipt scope, community retained-graph/access closure followed by scope-preserving terminal application, and media HTTP/moderation/worker regressions. This is source-local and H2 proof. Actual multi-service PostgreSQL delivery, normal scope relay, user-facing progress and the three-target recovery fixture remain unfinished; no deletion completion or recovery activation is claimed.

### Exact answer retraction

Saved-answer erasure now appends an ordered, ID-only retraction in the same agent transaction. Message tracks the exact answer that changed the question status. Retraction preserves a newer bound answer and human/closed outcomes, while a separate permanent answer cursor prevents a newer question revision from skipping the old answer's notification removal. Notification removes only the exact answer row, fences recreation and acknowledges the effect to agent. Resource cleanup remains pending until that acknowledgement commits. The same transaction publishes an updated source receipt to the existing lifecycle outbox; a completion-receipt insertion failure rolls all acknowledgement/cleanup changes back.

Legacy AI/human notifications used an indistinguishable shared row. Upgrade preserves IDs, read/done state and navigation, but removes preview/label text and replaces the title with "Question update". The repository also normalizes old leased events and direct writes. Affected unbound AI-derived statuses are conservatively invalidated without guessing an answer identity. New answer visibility uses live question access and the exact binding. An answer-local status event marker prevents duplicate repair after delivered payload erasure.

Focused common/auth/message/agent/notification checks cover queued and claimed acceptance, reordering, duplicates, later answers, legacy migration, visibility denial, rollback and source completion receipts. This is source-local migrated H2 and synthetic HTTP proof; cross-service PostgreSQL/native proof remains required. Account deletion/export frontend ownership is handed to #339 through `issue-251-frontend-contract.md`; server/resource public progress remains #251 until its GET contract is implemented.

Normal Study Server application now schedules bounded immutable Course/channel delivery through the existing outbox. Each self-command emits one page to five fixed dependents and its next-page command in the same transaction. Final verified import and READY receipt commit together; community retains exact source/kind/count/digest dispositions. Recovery mode does not start this ordinary relay. Eighteen focused common/auth/community/notification checks pass, including a 257-ID boundary, out-of-order and duplicate delivery, empty scope, receipt insertion rollback, original graph/scope recovery checks and public pending-delete startup. Cross-service PostgreSQL completion, retained payload cleanup and public source progress remain unfinished.

Public server/resource progress now uses the original requester identity recorded by the trusted owning source. The dedicated auth GET remains usable after source graph erasure, checks the live session and returns only target/progress fields. Six focused common/auth checks pass, including missing pre-dispatch progress, cross-account denial, spoofed identity, logout, immutable requester replay, source request rollback and actual canonical-fixture preparation. The frontend contract now includes this GET and explicit unavailable-yet behavior. Full user journey and source completion work remain pending.

Auth now erases ordinary profile/credential payloads under the account write lock and retains a non-login UUID tombstone plus existing restricted moderation records. Late reset/verification/signup notices, OAuth links and profile/password writes cannot recreate erased values. Token redemption takes user before token; operator authority uses its existing grant lock before account rows, including deletion. Appeal links are removed while already-preserved cases can still be reviewed without sending email to the deleted account. Thirty-three focused auth checks pass, including concurrent credential/cleanup ordering, transactional rollback, pending-email erasure, fixed moderation preservation and externally gated final account progress. This is source component proof; full PostgreSQL/browser/recovery union remains pending.

Community account cleanup now erases authored announcements, events, office hours and personal membership/presence rows while retaining exact content IDs for downstream erasure. Mutations take the terminal authority lock before graph/content locks. Shared courses remain under their current nonterminal server owner with explicit instructor reassignment; an older restored terminal owner stays inaccessible and pending. Forty focused community/media checks pass, including rollback, delayed creation, stale invitations and same-requester resource deletion retry after canonical closure and metadata erasure. Downstream payloads and receipts still prevent a completion claim.

The hosted dependency preview exposed an outdated export HTTP assertion. Permanent account closure intentionally returns 410 before session revocation is evaluated; the test now asserts that exact status while retaining login/refresh denial and unaffected-account checks. Seven focused export HTTP checks pass. The hosted cross-service preview must rerun at the corrected head.

Account-authored content now advances through bounded commands on the existing outbox. Each page carries at most 256 original-entry-bound identities to search and notification. Both recipients persist permanent content fences and append exact-command receipts atomically with erasure. The source verifies those receipts against every retained ID and destination event. Matching old outbox payloads become ERASED and cannot be reclaimed or manually replayed; a copy already in flight is suppressed by the recipient marker. Later human question updates preserve their IDs and read/done state with the existing neutral preview policy.

Focused checks cover a 257-ID page boundary, independent reordered batches, duplicate delivery, wrong producer/command, receipt insertion rollback, stale direct/durable/legacy writes and unchanged source PENDING state. Explicit producer completion, verified zero-content dispositions, remaining source retention and real PostgreSQL recovery/browser proof remain required. The journal/scope contracts and media V5 are unchanged. The hosted preview also exposed two agent fixture assumptions: late ingestion now asserts permanent 410, and the answer fixture names its original columns after the additive status-event column. All four focused agent checks pass without changing production behavior.

Independent review found that retiring a shared question-preview event could suppress another author's later, still-pending inbox update. New shared answered events now enter the source outbox with neutral text. Cleanup sanitizes older retained payloads through an immutable source revision cutoff in 64-row pages and preserves their delivery/lease state. A regression reproduces an update created after closure but before cleanup, verifies no inbox row existed, then delivers it through the real notification controller and confirms the update appears. Thirty-eight focused common/community/message/notification checks pass, including a 65-row sanitation boundary. Exact question deletion still retires its identity.

Shared question notification payloads now become neutral ID-only updates at the writer boundary. Cleanup neutralizes bounded older payload pages through the deletion cutoff while retaining their pending/claimed delivery and later human updates. Regressions cover a later pending human notification delivered after cleanup, 65 retained payloads, claimed retry, and source/recipient rollback. Full common and agent tests pass after isolating the terminal fixture database and awaiting actual bounded worker completion in the source-timeout regression.

Study Server DELETE delegates target moderation to first-request authorization. Original-requester retries resolve their committed job first, including after canonical moderation denial and graph erasure; other callers receive 404, first restricted requests still fail, and other routes remain closed. The real auth HTTP regression confirms canonical server entries immediately deny target moderation. Public endpoint/status/cookie contracts are unchanged. Final producer dispositions, retention aggregation and hosted cross-service proof remain open.

Explicit account-content completion now uses `ACCOUNT_CONTENT_FINAL` carrying `{entry,owner,contentCount,batchCount}` only after every owning batch has both destination acknowledgements. An empty producer sends `0/0`. Each search/notification recipient compares the totals with its committed original batch commands, records the producer disposition and returns `ACCOUNT_CONTENT_COMPLETE` with `{commandId,completion}`. Community, message, media and agent dispositions are required. Receipt append, recipient COMPLETE transition and its coordinator progress update are one source transaction; a failed update rolls all of them back. Missing producers remain PENDING. Final source receipts bind the original destination command UUID. Source-specific retained ownership, attribution and physical-object cleanup remain independent PENDING conditions.

The unshipped canonical helper now admits all seven owning application contexts for existing `events`/`deliver` actions. Account/server/resource seed and mutation actions retain their original source restrictions. Journal and scope schemas are unchanged; no new production endpoint, scheduler or retry queue exists.

The internal `ResourceLifecycle.MaintenanceDeletion` hook now requires the existing verified maintenance transaction and an exact terminal resource tuple. It rejects missing/stale state, unsettled writes and active leases, rolls back metadata if quota release fails, releases reservation once, and reconciles matching canonical targets. Focused media tests passed transaction enforcement, unknown-write/lease/stale-tuple rejection, actual constraint rollback and duplicate completion. V5 and public routes are unchanged. The hook does not prove remote deletion itself; #342 owns inventory/prefix and physical closure verification before calling it. Media remains PENDING until downstream and metadata disposition complete.

Independent completion-scope review found that community/message finals alone omitted media resource copies and agent answer notifications. The fixed producer set is now community/message/media/agent. Media retains exact uploaded RESOURCE IDs before physical cleanup; agent retains exact STUDY_ASSISTANT_ANSWER IDs from its durable retractions. Both use the same bounded source/recipient protocol. An actual search-store regression retains a resource and PENDING account through the other three finals, then proves media erasure/fence before its final allows COMPLETE. Full common/agent and focused migrated media/search/notification checks pass, including final receipt rollback. Source physical cleanup, question-status retractions and conservative attribution/accounting remain independently pending. The unshipped media V5/agent V14 additions do not change journal/scope wire schemas or media V6 ownership.
