# Issue #332 implementation evidence

Draft PR #336 owns this slice. The focused deployment/recovery suite passes 64 tests with no skips, including the actual encrypted restic fixture. Hosted results are tracked by the PR checks; they are not implied by the local proof.

The initial bounded implementation adds an encrypted terminal-journal replica and source-receipt recovery sequencing to accepted backup tooling. Design precedes implementation in `docs/architecture/current-authority-recovery.md`. The change preserves pgBackRest, restic, the deployment lock, existing release formats and source ownership. No provider account, public deployment, local HTTP server or browser was used.

The first validator tests failed without implementation, then passed a fixed digest generated by #251's actual Java `TerminalJournal` class. They reject altered entries, gaps, unsupported fields, duplicate targets, changed upper bounds and malformed timestamps. Replica tests began red and now verify write/read-back ordering, stable identity after lost acknowledgement, lower-bound enforcement, immutable full-page reuse and fixture/real-source separation. An added cross-page duplicate test caught manifest publication before complete validation. The candidate is now verified before publication; final persisted read-back still precedes acknowledgement.

Actual restic 0.19.1 fixture storage initializes an encrypted repository, writes and decrypts a complete prefix, lists the immutable manifest and revalidates it before acknowledgement. Repository files contain no plaintext canary. A wrong encryption key fails. The native fixture uses the official Windows archive verified against its published SHA-256; hosted AMD64/ARM64 fixtures use the accepted repository tool lock. These are storage fixtures, not independent provider durability or freshness evidence.

Recovery sequencing tests inject every omitted, failed or stale participant; wrong session/native invalidation authority; impossible cleanup counts; and an advancing external head. Each prevents a successful authority-stage receipt. Successful sequencing preserves pending/preserved disposition and keeps `publicCutoverAllowed` false. Repeated calls preserve source-owned invalidation identity and time. Injected responses do not establish actual PostgreSQL mutations.

The helper has fixed localhost routes, bounded stdin/stdout, no redirect/proxy support, generic errors and a whole-process deadline covering streaming stalls. It compiles with Java 21, and its native route/byte-bound contract passes without opening a local listener. The hosted-only fixture separately tests successful transport, redirect refusal, oversized output and streaming timeout. Explicit operator init/replicate commands verify the accepted deployment/configuration and use the existing lock. No recurring schedule or automatic public cutover is activated.

Actual #251 participant routes and durable invalidations, #249 authorization, isolated application startup, complete object consistency and retained backup policy remain dependencies. External journal protection against wholesale rollback, current operator authority and original-writer fencing remain unresolved provider/cutover gates. Full hosted review and exact-head verification are pending for this draft. Issue #332 remains open.

The initial draft passed the actual encrypted recovery and helper fixtures on both native architectures and completed full CodeAnt review without inline findings. Continued integration binds #251 journal version 2's explicit moderation-record retention policy; its updated fixed digest was reproduced by compiling the actual Java contract. Recovery mode now omits ordinary email, outbox, media, ingestion, rebuild and native-expiry worker beans. Focused Spring-context tests establish disabled workers and fail-closed malformed settings without starting local HTTP listeners. The pure isolated composition rejects historical capability, foreign release/volume identities and nonisolated database receipts. It contains only seven sources plus PostgreSQL, private loopback HTTP and an internal Docker network, with migrations, email, indexing, external error reporting and telemetry delivery disabled. The capability remains disabled in release policy until actual source integration and native isolation proof pass.

The operator checkpoint decrypts the exact database-bound configuration and reuses only a verified owned restore volume. Focused tests reject foreign images/labels/mounts, exposed ports, unexpected networks and failed helper execution before accepting isolation. An extra database mount initially passed the new rejection test and is now rejected. Failed attempts retain their recovery UUID, configuration, volumes and failed status. A successful fixed Java helper must establish refused TCP connections to every source; an execution error cannot count as isolation. The pure source-receipt module no longer claims Docker isolation, addressing the full review finding at `d887a355`.

The hosted-only worker fixture derives test images from the actual packaged applications, checks their resolved Spring beans for any remaining scheduled work before startup, then verifies private listeners and an internal network. It uses the already migrated smoke database and does not claim restored participant data proof. Release policy stays disabled. Local focused operator/configuration/runtime/receipt tests pass, and the new Java probe compiles without starting a listener. Actual packaged startup verification is pending for this checkpoint.

Schema validation is a separate required pre-start step. An ordering regression first failed because no source schema was checked. The operator now validates every source before starting any of them and rejects a single failed check. The fixed Flyway helper uses read-only PostgreSQL connections, refuses every non-success migration state and never calls migrate/repair. The hosted fixture includes changed-checksum, missing-version and ahead-version rejection, followed by successful restoration checks. The local focused deployment/recovery suite passes 72 tests with no skips, including encrypted restic; the schema helper compiles against the installed Flyway API. Hosted proof remains pending at this checkpoint.

The first actual packaged worker fixture reached normal staging successfully, then exposed a fixture image-build error: BuildKit interpreted a bare image ID in `FROM` as a registry name. The fixture now assigns a unique local tag, checks that it resolves to the exact accepted image ID and derives the test image without build networking or forced pulling. This changes only the hosted test image preparation; it is not application-recovery proof until the fresh native run succeeds.

A no-database Java contract test against the actual installed Flyway dependency caught that `none` is interpreted as a logger class name. The configuration now supplies an empty logger list. The test exercises the actual helper configuration, confirms bounded read-only settings and requires exact safe stdout after submitting a diagnostic canary. It passes without starting a local listener and also runs against the actual packaged auth dependencies in the hosted fixture.

The actual packaged fixture subsequently passed all seven source schema checks and changed, missing and ahead-version refusal on both native architectures. Auth, community and message passed recovery startup without ordinary workers. Media then failed startup. Code inspection identified required constructor directories removed with the normal volumes and the fixture's local namespace conflicting with regenerated production S3 settings. Recovery now supplies only bounded private scratch at the existing legacy/spool paths; the hosted fixture retains its verified local backend. Production stays S3, whose constructor configures its client and checks the database namespace without making a provider request. Bounded exception types/frames and exit/OOM state now diagnose a remaining startup failure without printing settings. Empty scratch never proves object erasure; #251's media replay must preserve pending physical cleanup. The complete seven-source startup result remains pending after this correction.

At `94fc7fab`, both native packaged fixtures passed all seven source startups, ordinary-worker absence, loopback-only HTTP and schema checks, including changed/missing/ahead-version rejection. Full CI, packaged security and completed full CodeAnt review also passed. The local recovery suite passed 72 tests with no skips, including actual encrypted restic. This establishes the corrected packaged activation boundary. It does not establish nonempty source terminal replay, independently retained current scope, object recovery or public cutover. The next scope-attachment integration keeps the capability disabled and must repeat exact-head verification.

The independent resource-object byte archive began with failing tests before implementation. Four tests now pass without skips, including an actual encrypted restic repository. They cover binary byte preservation, fixed private stdin and snapshot references, canonical key/hash/size and settled-write checks, deleted-resource refusal, short/oversized/wrong-content read-back, same-size snapshot substitution and the wrong encryption key. No plaintext canary appears in repository files. The module returns a private byte-evidence reference only after full read-back; no media adapter, source inventory API, provider account or recovery activation is implemented. Native recovery jobs include this real fixture on both architectures.
