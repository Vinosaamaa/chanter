# Issue #59 — CodeRabbit fix log (PR #76)

## Pass 1

| Comment | Action |
|---------|--------|
| Duplicate auth-derived CTA logic across components | Fixed — `useStudyServerCta()` hook |
| Docs link should use canonical README URL | Fixed — `MARKETING_DOCS_URL` |
| Explicit types for use cases / pricing teaser | Fixed — `string[]` and `MarketingPricingTeaser` |
| Missing authenticated CTA test | Fixed — `LandingPage.test.tsx` |

## Deferred

None.
