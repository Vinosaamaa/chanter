# Issue #247 implementation evidence

PR #330 replaces production hashing and Java-wide vector scans with the pinned all-MiniLM-L6-v2 ONNX model and authorized pgvector queries. Explicit API models are optional. No provider fallback or paid verification calls are used. Agent migration V13 follows accepted #316 V11/V12. The #252 owner supplies production pgvector installation and extension bootstrap; epoch 8 and final production configuration follow that union.

The model adapter tests first failed without an implementation, then passed real paraphrase ranking, bounded input and asset-integrity rejection. Version-store tests first failed without the schema/store, then established immutable model dimensions, coverage-gated activation and rollback. Scoped SQL tests reject wrong Course, Study Server, Cohort, source checksum, parser version and dimensions before returning evidence. Native PostgreSQL repeats those contracts on AMD64 and ARM64 with the supported extension.

The production router pins one model per operation. Durable rebuild jobs persist progress, generation-bound leases and bounded retries. Tests cover an expired lease, discarded candidate, source replacement and a genuinely paused provider whose resource is deleted before computation resumes. Deletion completes while inference is paused, removes all retained vectors/jobs and prevents late publication. Ingestion and backfill retain short final transactions rather than holding resource locks during model inference.

Empty vectors and provider outages initially fell back to downloaded resource keyword matching. Regressions exposed that behavior; the fallback and whole-vector repository API are removed. Unavailable semantic retrieval yields no resource citations and recommends human support. Authorization and cancellation errors propagate rather than masquerading as provider outages. The complete local common/agent suite passed 227 tests with 30 native/environment-gated skips before the final cancellation regression. Interrupted ONNX/API calls then reproduced an incorrect outage conversion; both adapters now preserve cancellation, with real-model and loopback-adapter regressions.

The complete native workflow passes on AMD64 and ARM64 at `6e1d8626`. The actual agent image runs read-only and unprivileged at 640 MiB, sharing two CPUs with the 896 MiB PostgreSQL fixture. On 100,000 generated chunks, AMD64 inference p50/p95 was 4.4/5.4 ms and database p50/p95 was 17.5/21.5 ms; ARM64 was 9.0/10.1 ms and 19.0/21.5 ms. Combined p95 was 26.0/31.7 ms respectively. Peak container memory was 367,452,160/362,180,608 bytes. Full 256-token inference p95 was 87.0/149.6 ms and four-client retrieval p95 was 162.3/92.3 ms. Both preserved the actual scoped query plan and verified valid indexes under a non-superuser schema owner. Plan capture uses process output because the stopped container's temporary filesystem is gone.

The relevance fixture ranks the correct source first for all ten paraphrases. Eight exceed the unchanged 0.35 evidence threshold; illness-on-exam and FIFO wording require handoff. The unrelated football question remains below 0.143. The load corpus repeats those ten semantic vectors across 1,000 resources and ten courses. It establishes scoped query and memory behavior, not broad educational answer accuracy.

Native libraries are extracted into the immutable image during packaging, preserving non-executable scratch. The real image caught default JNI extraction failure and a tokenizer directory-override defect; an explicit immutable library file resolves both. Private media's fresh vector extension path and packaged dependency security pass at `1cd19ae2`. Full CI, including actual product journeys, passes at `1cd19ae2`. The final #252 union and exact-head CI/security/native/full-review gates remain required before acceptance. Issue #247 stays open through merged-main and production verification.
