# Issue #247 review dispositions

Completed full reviews cover `14446eb`, `e8d988c`, `caef6171` and the accepted production union at `4260537b`. Quality/SAST statuses alone do not establish review completion.

- Zero vectors from the former default hashing embedder: production hashing has been removed and is explicitly rejected by provider configuration. The real pinned ONNX model produces valid normalized coordinates for the punctuation fixture. Tokenless queries return no evidence before inference. The datastore continues rejecting zero/nonfinite vectors; the explicit API adapter also rejects them. Hashing remains a deterministic test-profile fixture and is not silently substituted in production.
- Candidate preparation could only use the active model: the pipeline now exposes preparation for a registered target model, and a durable resource/model worker fills candidates while active ingestion continues. Tests cover candidate coverage, activation/rollback, bounded retries, lease expiry, discarded candidates and replacement/deletion during delayed provider work.
- Model metadata length bounds disagreed with storage: ID/provider/model/revision validation now matches the database limits, with regression coverage. Model IDs also use a restricted character set before creation of a model-specific partial index.

Independent system review removed downloaded-resource keyword fallback after empty or failed semantic retrieval, removed the whole-vector repository read API, bounded retained versions to active/candidate/previous, and retained the short generation/lease-checked final transaction. Native image verification exposed two runtime integration defects: JNI extraction into non-executable scratch, and the tokenizer's directory-override null handling. Native libraries are now loaded from immutable image assets using an explicit library file for the tokenizer. Both corrections now pass in the actual native image on AMD64 and ARM64 at `6e1d8626`, including long-input and four-client load.

No broad suppression or authorization/data-loss deferral is used. Production pgvector bootstrap and epoch-8 integration include accepted #252. Native memory/load with successful telemetry export is verified on both architectures. Exact-head release/security/CI/full-review gates remain mandatory for acceptance.

The completed integrated full review at e8d988c produced seven further findings. The accepted #252 union removes the production hashing override and sets only the agent's packaged model path. Deployment regressions verify explicit API configuration survives and epoch 8 prevents downgrade. Native workflow path coverage now includes Java image/runtime-lock changes. Non-agent image builds use an independent default build context input; only agent packaging supplies model assets. Version-store tests explicitly restore their active fixture model. Retrieval catches only the dedicated semantic availability outcome; authorization, validation and cancellation propagate, with a no-saved-answer regression. Startup model-specific index DDL uses the service's existing schema ownership; the native image proof uses a role without superuser/create-role/create-database privileges. Rejection of the obsolete keyword mode is an intentional epoch-8 compatibility change; restoring keyword resource fallback would violate #247. The runbook documents removal of old provider/keyword configuration.

The final cancellation check reproduced two adapter failures and a retrieval-boundary failure: CancellationException is an IllegalStateException subtype, so its outcome must be preserved before the provider-outage catch. Both adapters now preserve the interrupt flag and cancellation before/after inference. The retrieval service propagates that outcome, and the HTTP boundary regression confirms it cannot create a saved answer. Real-model, API, current-authorization and answer-persistence regressions pass.

The accompanying review suggestions were checked too. V13 creates a real vector column, demonstrated by the native image; the BYTEA probe warning does not match the migrated schema. The outage smoke test deliberately throws the availability result from the retrieval spy, while the lower-level provider-outage test exercises the adapter boundary. The cancellation fixture now retains and verifies its retrieval mock. The load proof checks both chunks and lifecycle rows are empty and explicitly requires a valid HNSW index. PostgreSQL may choose the more selective scope B-tree and exact scoped sort; the captured plan ranks only 3,000 authorized chunks out of 100,000, with measured latency below the acceptance budget. Forcing an approximate global scan would weaken that evidence.

Custom optimization suggestions remain non-blocking: at most three versions retain vectors, each model call is bounded, and the measured retrieval budget includes current SQL configuration round trips. API/JDBC batching, merged control/status queries, alternate array transport and fixture/helper extraction can be evaluated when a measured bottleneck justifies them. The current deletion transaction intentionally owns chunk/vector/job cleanup together. Model-specific HNSW indexes and narrow availability handling are already implemented. The native fixture intentionally pins the 384-dimensional model. Separate test/package stages and independently checksum-verified model downloads remain explicit proof setup, not runtime network behavior.
The completed full review of `caef6171` finished on 2026-09-19. Invalid similarity thresholds now fail during construction, with NaN, infinity and out-of-range regressions. Missing serving providers already fail startup, and private stage/activate controls reject unconfigured clients before state changes; this is an invalid deployment configuration, not a request-time provider outage. The provider-outage smoke test now explicitly verifies its retrieval spy, and a separate datastore exception case exercises the availability boundary.

The reported 351 MiB and 163 ms values are observations from one native run, not required ceilings. The enforced acceptance limits are the existing 640 MiB container allocation and two-second p95 for both sequential and four-client retrieval; actual figures remain reported independently. The final union also measures enabled telemetry exporter overhead. Native cleanup belongs to the workflow's always-run owned-container step; the helper intentionally leaves its container available until evidence is collected. Each hosted database/schema is a fresh isolated fixture. The removed content-client mock was obsolete. Clearing pgvector OPTFLAGS removes host-specific CPU flags while retaining PostgreSQL's normal optimized build flags, which keeps the binary portable. Further batching, caching, helper extraction and build-time download streaming are optional optimizations, not unresolved correctness findings.

The completed `4260537b` review adds no inline defect. Its API-only/offline release suggestion does not match the supported bundle: every release includes the free local default, while runtime configuration can explicitly select API models. The downloader reuses checksum-valid seeded assets without network access; a cold build already needs pinned external image/tool dependencies and does not promise offline operation. The bounded build-time buffers never run inside the production agent. BuildKit caching, a shared Compose fragment, a smaller context, combining Maven phases, native-extraction helpers and fewer fixture insert passes are optional build improvements. Normal Compose builds reuse cached layers and keep the required extension current. Catalog batching and fixture builders are also optional; current live authorization remains mandatory. The HNSW/index, portable compiler flags and measured-ranking suggestions are covered above. Documentation explicitly calls 10/10 an observed fixture result; the gate requires at least 9/10 ranking and 8/10 supported answers. No runtime threshold or privacy check was relaxed.
