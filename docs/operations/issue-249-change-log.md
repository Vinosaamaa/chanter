# Issue #249 change log

The implementation scope is recorded on #249. Work starts from accepted main `5d3b154` in the dedicated moderation branch. The initial read found partial blocking and friend-presence filtering, but no unblock, reports, platform roles, privileged audit or account suspension state.

The design keeps moderation inside auth-service with source-owned evidence checks, separately granted operator roles and reversible restrictions. Gateway routing and admission belong to #253. Session-bound token/introspection changes belong to #316 and must be preserved. No production operator enrollment or public release is claimed.

The first TDD slices add separate operator grants, authenticator enrollment and replay protection, encrypted factors, assigned-case evidence reads, audit records and timed restrictions. Current status checks protect auth session issuance/refresh and the authenticated handlers in seven services. Realtime revalidates incoming actions and idle connections. A close-frame race found by the real WebSocket test was fixed before proceeding.

The report client uses fixed source-service endpoints and bounded evidence snapshots. Message evidence requires a saved DM participant or current channel permission. Blocking does not erase the participant's right to report historic DMs. The foundation checkpoint compiles every backend and passes 30 focused tests, including three real WebSocket close repetitions; one PostgreSQL-only test is skipped locally. PostgreSQL audit/note immutability has a dedicated real-database regression; its hosted execution is still pending. Other source integration, restriction actions, appeals and UI remain in progress. The complete acceptance boundary is tracked in the system review.

The next slice adds current-member Study Server evidence, case-bound restriction/reinstatement with exact target confirmation and atomic audit, durable email action notices, and verified-email appeals for passwordless/provider accounts. Appeal links are one-use, expire after 20 minutes, never issue sessions and require restriction ownership. Exact gateway exceptions retain sensitive limits and browser origin/CSRF protection. Focused auth/community/gateway checks pass. The branch incorporates accepted #253 before this slice; #246 integration follows at a clean checkpoint.

After integrating accepted #246, resource metadata evidence now checks the reporter's current course scope. Public reads and worker ingestion check current resource/Study Server restrictions before and after storage reads. Batched source decisions hide quarantined resources without hiding unrelated resources on the same page. Focused resource, download/deletion and evidence tests pass.

The media boundary now has a signed-token signaling guard and bounded participant reconciliation. A real native Caddy/LiveKit test proves server removal, transport closure and reconnect denial with the original unexpired token. The isolated test passes locally and has an additive Linux CI step with pinned archive checksums. Database suspension and published audio/browser proof are still required; production ingress remains owned by the deployment lane.

Message-service now serializes friend acceptance, messaging, blocking and unblocking on the same canonical user pair. Focused tests first reproduced the concurrent block/write race, blocked-request acceptance and missing unblock/current-peer checks, then passed with the changes. Unblocking removes only the caller's block. Existing saved messages remain intact. Accepted #316 session semantics will be integrated at this checkpoint.

Accepted #316 is now integrated. Operator verification and realtime use its live session introspection, which additionally rejects suspended accounts. Operator search, scoped report paging, appeal resolution and role controls are implemented. The safety and appeal pages retain draft input on unconfirmed requests. Context links carry exact saved source IDs. Focused interface tests and lint pass; production asset checks retain the original core limits with a separately measured deferred moderation allowance.

The next enforcement tests reproduced quarantined history/detail leaks, stale channel subscriptions and delivery of a previously saved DM after blocking. Current source filtering and authority checks now pass those regressions without deleting evidence. Study Server restrictions cover public course/cohort/channel aliases and aggregate navigation sources. Periodic call reconciliation ends newly ineligible calls, and replacement presence snapshots clear stale online peers. Full verification exposed a missing router wrapper in resource tests and an analytics controller fixture using live authority; both fixtures were corrected. Hosted combined database/Redis/LiveKit/Caddy/browser proof is next. No local preview stack or browser attachment was started for that proof.

The first complete CodeAnt review identified restricted-resource deletion, uppercase UUID lookup, delayed operator directory results, hidden realtime failures and concurrent pending appeals. Each now has a regression and a fix. Source restriction pages use one bounded database query; verified appeal issuance prunes expired credentials. The hosted product proof passed ordinary journeys and explicit administrator bootstrap, then caught the missing course-channel Study Server field. The authoritative field now reaches message-service, and the proof creates a discussion in the learner's enrolled cohort. Focused backend checks, all 325 frontend tests, lint and the production build pass. Combined audio, appeal, network and pixel acceptance is still pending; review dispositions are recorded in issue-249-codeant-fix.md.
