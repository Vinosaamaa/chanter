# Issue #332 review dispositions

The completed full CodeAnt review at `d8fa5f65` reported no inline findings. Both native recovery architectures passed encrypted PostgreSQL restore, the actual encrypted terminal prefix fixture and bounded localhost helper transport. That checkpoint predates journal version 2 and worker-isolation integration; it does not approve later changes or complete whole-application recovery.

Independent implementation review identified two blockers to actual source startup. Historical images ignore a new recovery flag, so the isolated composition now requires an explicit release capability before starting any service. Its build policy remains disabled until all participant handlers and native isolation proof are accepted. Ordinary background jobs must also remain absent while restored authority is incomplete. A common recovery-mode condition omits worker beans, including separated native expiry scheduling, while leaving repositories and private handlers available.

The shared #251 protocol now requires version 2 and the explicit moderation-record preservation policy in every entry and digest. Missing policy and version 1 fail closed. The fixed reference digest matches the updated actual Java implementation. Manifest and source-receipt schemas remain independently versioned.

Full review and exact-head gates must repeat after these changes and subsequent accepted source integration. Provider freshness, retained objects and original-writer fencing remain explicit open acceptance, and public cutover remains disabled.

The full review at `d887a355` correctly identified that receipt verification alone cannot claim runtime isolation. The lower-level module now returns `authority-receipts-verified` with `isolationVerified: false`. Only the operator command can return an isolated status after checking actual container image/label/mount/network ownership, absence of published ports, internal networking and loopback-only source access, then stopping its verified owned containers. Both result types retain `publicCutoverAllowed: false`. Regression tests distinguish those contracts.

At `cbec9afe`, full review found that the new operator lacked a host-architecture check. A failing test confirmed configuration decryption could begin for a foreign architecture. The operator now rejects before decryption, Docker or creating its lock. The review also identified missing standalone recovery workflow paths for new Java helpers, the native startup wrapper and Java Dockerfile. Those paths now trigger it explicitly. The release workflow already covered these paths and remains the owner of actual packaged worker-startup verification. Neither workflow result substitutes for the actual accepted seven-source recovery proof.

The `1906caba` fixture-restart suggestion is inapplicable. The fixture requires Linux CI and an exact owned randomized smoke-project name. It runs last, and `smoke-release.sh` removes that ephemeral project in its EXIT trap. Restarting the old application would add no recovery evidence and would resume ordinary work on the fixture database.

The same review raised a container/volume replacement race. The operator now pins the full inspected Docker container ID for stop; a regression proves disappearance of that ID cannot fall back to a reused name or reach startup. The original container is stopped, never removed, so its reference keeps the external volume in use. Actual post-start checks require exact images, mounts and networks before authority replay and again before the isolation receipt. Independent review agrees that operation requires an exclusive trusted Docker host. A Docker-privileged actor can replace or modify any state and is outside this trust boundary; no claim of protection from malicious host-root or independently fenced original writers is made. Public cutover remains disabled.

The full review at `cce1bd1f` found that post-start networking checked only the internal flag. A regression confirmed that foreign network labels passed. The operator now requires exact recovery, Compose project and network labels, pins the full network ID immediately after PostgreSQL startup and checks each source's attached network ID. It repeats ownership and identity checks before replay and before the final receipt. Tests reject changed labels, name, internal flag and replacement identity before participant calls. The exclusive trusted-host requirement still applies.

Two fixture cleanup comments describe permanent growth from copied libraries and derived image tags. The fixture runs once in each disposable GitHub-hosted job, is guarded by Linux CI and exact ephemeral smoke ownership, and is not included as an operator command in the release. The job discards its scratch directory and Docker storage. There is no persistent production accumulation or repeated fixture loop. Preserve failed job material for its bounded lifetime; adding broad Docker or filesystem pruning would not improve this proof.

Independent packaging review found that the release publication archive used a separate explicit file list and omitted the newly copied recovery modules. That list now includes the operator, composition and terminal-journal dependencies. The parent lane's additive error-reporting and heartbeat modules must be preserved during union. The media-owned object recovery design is coordinated with #251 and accepted as a contract only; no inventory route, provider version or object cutover capability is claimed.

Full review completed at `43cd8ecf`. The new mutable `setup-java` finding is valid: the recovery workflow now uses the same exact v5 commit already pinned in the production release workflow. Repeated architecture/path-filter comments refer to fixes already present, and the disposable fixture cleanup dispositions above still apply. Both encrypted PostgreSQL recovery jobs passed at that head. Release merge-preview correctly rejected community's newly accepted `LiveMediaReconciler` scheduled bean before isolated startup; it needs the existing recovery guard during accepted-main integration. This failure does not weaken or replace the earlier `94fc7fab` activation proof.

Full review completed at `dde35ebb`. Its failure-cleanup finding is valid: the operator reread container labels without retaining the pinned network ID. A regression reproduced stopping a substituted network attachment after failed source replay. Cleanup and retry stops now require the already verified network identity and each remaining owned container's attachment, while allowing a partially started set. An unverified replacement is preserved for private inspection rather than stopped. The network-name observation describes two intentional phases: the physical recovery receipt records its original `-network`; the detached database later joins a separately owned, explicitly inspected `-authority` network. This distinction is now commented and tested. The pagination suggestion is inapplicable: `total` changes once after a complete kind, while each page only compares the unchanged aggregate plus its declared count. A 130,000-ID, 508-page scope regression passes below the aggregate cap without repeated counting.
